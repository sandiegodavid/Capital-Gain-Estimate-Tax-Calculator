document.addEventListener("DOMContentLoaded",()=>{const $=id=>document.getElementById(id),bindDialog=(openId,dialogId,closeId)=>{const open=$(openId),dialog=$(dialogId),close=$(closeId);if(open&&dialog&&close){open.addEventListener("click",()=>dialog.showModal());close.addEventListener("click",()=>dialog.close())}};bindDialog("show-tax-formula","tax-formula-dialog","close-tax-formula");bindDialog("show-tax-rules","tax-rules-dialog","close-tax-rules");const status=$("source-folder-status"),sourceValue=$("source-folder-value"),sourcePath=$("source-folder-path");window.addEventListener("message",event=>{if(event.origin!==window.location.origin||event.data?.type!=="finder-result"||!status)return;status.textContent=event.data.message;status.classList.toggle("error",!event.data.ok);if(event.data.source){if(sourceValue)sourceValue.value=event.data.source;if(sourcePath){sourcePath.textContent=event.data.source;sourcePath.title=event.data.source}}if(event.data.setup_parent){const form=$("setup-realized-gains-form"),parent=$("setup-realized-gains-parent"),year=$("setup-realized-gains-year"),saleYear=$("sale-year");if(!form||!parent||!year)return;const button=document.createElement("button");button.type="button";button.textContent="Set up folders";button.addEventListener("click",()=>{parent.value=event.data.setup_parent;year.value=saleYear?.value||String(new Date().getFullYear());form.requestSubmit()});status.replaceChildren(document.createTextNode("Create a standard Realized Gains folder here? "),button)}});const form=document.querySelector(".tax-form"),output=$("tax-estimate-output"),stale=$("tax-estimate-stale"),stateDependents=$("state-eligible-dependents"),key="capital-gain-estimate-scroll-y",saved=sessionStorage.getItem(key);let timer;const clear=()=>{clearTimeout(timer);if(output&&stale){output.hidden=true;stale.hidden=false}},submit=()=>{clearTimeout(timer);if(form?.checkValidity())form.requestSubmit()},queue=()=>{clearTimeout(timer);timer=setTimeout(submit,350)};if(saved!==null){sessionStorage.removeItem(key);requestAnimationFrame(()=>scrollTo(0,Number(saved)))};form?.querySelector("#state-residence")?.addEventListener("change",()=>{if(stateDependents)stateDependents.value="";clear()});form?.querySelector("#filing-status")?.addEventListener("change",clear);form?.querySelectorAll("#qualified-children,#other-dependents,#state-eligible-dependents,#ordinary-income,#short-term-carryover-loss,#long-term-carryover-loss").forEach(control=>{control.addEventListener("input",queue);control.addEventListener("change",submit)});form?.addEventListener("submit",()=>sessionStorage.setItem(key,String(scrollY)));const toggles=[...document.querySelectorAll("[data-source-toggle]")];toggles.forEach(toggle=>toggle.addEventListener("change",()=>{const selected=toggles.filter(item=>item.checked);if(!selected.length){toggle.checked=true;return}const url=new URL(location.href);url.searchParams.set("source_selection","1");url.searchParams.delete("included_source");selected.forEach(item=>url.searchParams.append("included_source",item.value));url.hash="included-sources";location.assign(url)}))});
